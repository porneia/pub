{% if projects %}

    <p>
    <ul class="posts">
{% for project in projects %}
    <li><a href="{{ project.date | date: "%Y" }}.html#{{ project.date | date: "%m%d" }}">{{ project.date | date: "%Y/%m/%d" }}</a> &raquo; {{ project.title }}
{% endfor %}
    </ul>

{% endif %}